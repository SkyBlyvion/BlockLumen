/**
 * STORAGE_KEY : clé utilisée pour stocker la session utilisateur (token + profil) dans localStorage.
 * Sert à restaurer la connexion automatique au démarrage de l’application.
 */
export const STORAGE_KEY = 'blocklumenSession';
